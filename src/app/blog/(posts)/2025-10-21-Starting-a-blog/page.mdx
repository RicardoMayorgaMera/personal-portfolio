export const metadata = {
    title: "Starting my own blog",
    description: "How and why I started this blog.",
    author: "Ricardo Mayorga Mera",
    date: "2025-10-21",
    tags: [""],
    published: true,
};

import {Container, Word} from '../../namanTitle.tsx';

import path from "path";
export const __dirname = path.dirname(new URL(import.meta.url).pathname);

<Container path={__dirname}>
    <Word scale={140}>Starting</Word>
    <br/>
    <Word scale={45} italic>my</Word>
    <Word scale={70}>own</Word>
    <Word scale={75}>blog</Word>
</Container>

I realise this site might look a bit similar to someone else's 👀

# Heading 1
```typescript
const random = new Random()
```

```bash
export FQDJKLM_DFQ = DFJLQFD
```

I wanted to write something but then I thought `if(code) then code` and then I knew it was right
## Heading 2